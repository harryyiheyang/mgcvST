% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphical-susie.R
\name{graphical_susie}
\alias{graphical_susie}
\title{Estimate a Graph-SuSiE network from an mgcvST fit}
\usage{
graphical_susie(
  fitmgcvST,
  group,
  features = NULL,
  L = 5L,
  max_iter = 100L,
  max_sweeps = 10L,
  susie_tol = 0.001,
  residual_floor = 0.1,
  coverage = 0.95,
  min_abs_corr = 0.5,
  workers = 10L,
  BPPARAM = NULL,
  seed = 1L,
  glasso_tol = 1e-04,
  glasso_maxit = 10000L,
  retain_model_init = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{fitmgcvST}{An \code{mgcvST_model_fit} returned by
\code{mgcvST.estimate(Y, model.set(...))}.}

\item{group}{Non-empty character vector naming the score-component groups
used to construct the network covariance. This argument has no default.}

\item{features}{Optional character feature IDs or integer feature indexes
defining the network nodes. \code{NULL} uses every feature with a complete
compact working model.}

\item{L}{Positive maximum number of SuSiE single effects per node. The
effective value is at most the number of other network nodes.}

\item{max_iter}{Positive maximum number of IBSS iterations in each node fit.}

\item{max_sweeps}{Positive maximum number of randomized serial sweeps after
the parallel initialization.}

\item{susie_tol}{Positive convergence tolerance passed to \code{susie_ss()}.}

\item{residual_floor}{Non-negative lower bound for the residual variance,
expressed as a fraction of the observed pseudo-response variance.}

\item{coverage}{Credible-set coverage passed to \code{susie_ss()}.}

\item{min_abs_corr}{Minimum absolute within-set correlation used for SuSiE
credible-set purification.}

\item{workers}{Positive number of workers used when \code{BPPARAM} is \code{NULL}.
The default is 10 and is capped by the number of nodes.}

\item{BPPARAM}{Optional \code{BiocParallelParam} for the first scan. \code{NULL}
selects a SOCK backend on Windows and a multicore backend elsewhere.}

\item{seed}{Integer seed controlling randomized sweep order.}

\item{glasso_tol}{Positive glasso convergence multiplier. It is used by the
one fixed-support \code{glasso} initialization and by the subsequent coordinate
sweeps with the glasso stopping rule: the mean absolute parameter change
must be smaller than \code{glasso_tol} times the mean absolute off-diagonal
score covariance.}

\item{glasso_maxit}{Positive maximum iterations for the fixed-support
\code{glasso} initialization.}

\item{retain_model_init}{Logical; retain the minimal warm-start objects for
all final node fits.}

\item{verbose}{Logical; print compact progress messages.}
}
\value{
An object of class \code{mgcvST_network} containing the selected groups
and features, score covariance, final covariance and precision,
purified-CS AND adjacency, directed support and credible sets, the initial
fixed-support \code{W0} and \code{Theta0}, convergence trace, timings, and settings.
}
\description{
Constructs score coordinates for explicitly selected spatial score
components, runs the first nodewise SuSiE scan in parallel, and uses the
purified credible-set AND support in one fixed-support \code{glasso} fit. The
returned covariance and precision from that fit are retained as \code{W0} and
\code{Theta0}. Subsequent sweeps visit all nodes in a newly randomized order and
update the full nodewise SuSiE regressions with warm \code{model_init} objects.
No further \code{glasso} refit is performed.
}
\details{
\code{group} is deliberately required. A fit constructed with
\code{model.set(setting = "global_local")} contains separate \code{global} and
\code{local} score-coordinate groups; they are never combined by default.
Network edges are defined by purified SuSiE credible-set membership in both
nodewise directions.

A negative residual-variance estimate defines a zero-edge result for that
node, and the remaining nodes continue. During serial sweeps, a proposed
node update with a non-positive conditional variance is not inserted into
the covariance matrix; that node retains its preceding state and the sweep
continues.
}
