% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-set.R
\name{model.set}
\alias{model.set}
\title{Construct a reusable mgcvST model setting}
\usage{
model.set(
  formula,
  data,
  basis,
  family = mgcv::nb(),
  setting = "global",
  coordinates = c("x", "y"),
  ...
)
}
\arguments{
\item{formula}{A two-sided mgcv-style formula.}

\item{data}{Model data with one row per spatial observation.}

\item{basis}{A prepared \code{\link[=spde_basis]{spde_basis()}} object.}

\item{family}{An mgcv family object. The default is \code{mgcv::nb()}.}

\item{setting}{The model uses one \code{"global"} spatial score process.
\code{setting = "global_local"} is not supported.}

\item{coordinates}{Character vector naming the two coordinate columns.}

\item{...}{Additional arguments passed to \code{mgcv::gam(..., fit = FALSE)}.}
}
\value{
An object of class \code{mgcvST_model} for \code{\link[=mgcvST.estimate]{mgcvST.estimate()}}.
}
\description{
Builds the reusable \code{mgcv::gam(..., fit = FALSE)} setup consumed by
\code{\link[=mgcvST.estimate]{mgcvST.estimate()}}. The response name on the left-hand side is a label: it
need not be a column of \code{data}, because the helper replaces it with an
internal response bridge before constructing the mgcv setup. Linear terms,
factors, interactions, offsets, and additional mgcv smoothers on the
right-hand side are retained.
}
\details{
\code{basis} is one object returned by \code{\link[=spde_basis]{spde_basis()}}, and the model carries one
global spatial score process. Other user smoothers contribute nuisance
covariance to the marginal working model but are not score directions.
}
