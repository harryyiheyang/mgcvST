% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mgcvst-api.R
\name{mgcvST.estimate}
\alias{mgcvST.estimate}
\title{Estimate compact marginal-screen and covariance working summaries}
\usage{
mgcvST.estimate(
  Y,
  G,
  feature_id = rownames(Y),
  BPPARAM = BiocParallel::SerialParam(),
  chunk_size = NULL,
  source_files = NULL,
  worker_init = NULL,
  marginal_test = NULL,
  marginal_args = list(),
  method = "REML",
  control = mgcv::gam.control(nthreads = 1L),
  geometry_tol = 1e-09,
  ...
)
}
\arguments{
\item{Y}{Numeric feature-by-observation matrix.}

\item{G}{A reusable setup returned by \code{mgcv::gam(..., fit = FALSE)}. Its
response is replaced by each row of \code{Y}.}

\item{feature_id}{Unique feature identifiers. Defaults to \code{rownames(Y)} or
sequential identifiers.}

\item{BPPARAM}{A \code{BiocParallelParam}; defaults to \code{SerialParam()}.}

\item{chunk_size}{Positive number of genes per task. The default creates at
most one chunk per worker, limiting repeated serialization on SOCK
workers.}

\item{source_files}{Ordered R files, or directories containing R files, to
source once per worker. Use this for sourced custom S3 smooth methods.}

\item{worker_init}{Optional zero-argument initialization function run once
per worker after \code{source_files}.}

\item{marginal_test}{Optional function implementing the corrected marginal
spatial score interface of \code{mgcv.taps::taps_score_test()}. \code{NULL} locates
a sourced \code{taps_score_test()} or the installed \code{mgcv.taps} export on each
worker.}

\item{marginal_args}{Named list of additional marginal-score arguments.
\code{fit}, \code{test.component}, \code{method}, and \code{n_threads} are controlled by
mgcvST; Davies is primary, Liu is the explicit numerical fallback, and
score threads remain one.}

\item{method}{Fitting method passed to \code{mgcv::gam()}. \code{"REML"} is the
default.}

\item{control}{An \code{mgcv::gam.control()} object. Internal thread counts are
always forced to one.}

\item{geometry_tol}{Positive relative tolerance used to verify shared row,
basis, fixed-design, and unscaled precision geometry.}

\item{...}{Additional arguments passed to \code{mgcv::gam(G = G, ...)}.}
}
\value{
A compact object of class \code{mgcvST_fit} containing marginal score
p-values, Wood audit values, feature IDs, working errors and variances,
field scales, shared geometry, timing and convergence diagnostics, and an
explicit failures table.
}
\description{
Fits each row of \code{Y} with a reusable \code{mgcv::gam(fit = FALSE)} setup and
computes the corrected marginal spatial score used for feature screening,
and retains only the fixed numerical summaries needed by \code{\link[=mgcvST.test]{mgcvST.test()}}.
The default marginal test is \code{mgcv.taps::taps_score_test()}, or a sourced
\code{taps_score_test()} found on the worker. Full \code{gam} objects are never
retained. Genes are processed in chunks through \code{BiocParallel}; the default
is serial. On Windows, use a persistent \code{SnowParam(type = "SOCK")} object
and pass the same object to estimation and testing.
}
\details{
\code{source_files} supports source-first custom smooths. Each SOCK worker
sources the ordered files once, before it evaluates a chunk. Multicore
children inherit the parent state, while the same guard remains harmless.
Every worker sets common BLAS/OpenMP thread controls to one, and \code{control}
is forced to \code{nthreads = 1} and \code{ncv.threads = 1}.

Keep \code{SerialParam()} for reproducible small jobs. On Windows, the standard
parallel backend is a persistent \code{SnowParam(type = "SOCK")}. On a
single-node Linux Slurm allocation, prefer \code{MulticoreParam()} when forking
is available; do not force SOCK workers on Linux. Persistent SOCK workers
are useful when fork is unavailable and their startup cost is amortized
across a large scan.
}
