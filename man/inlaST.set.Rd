% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inla-api.R
\name{inlaST.set}
\alias{inlaST.set}
\title{Prepare a sparse INLA estimator for mgcvST}
\usage{
inlaST.set(
  formula = NULL,
  data = NULL,
  basis = NULL,
  family = mgcv::nb(),
  setting = "global",
  coordinates = c("x", "y"),
  precision_scale = c("raw", "observation"),
  G = NULL,
  control = list(),
  ...,
  mesh = NULL,
  kappa = NULL
)
}
\arguments{
\item{formula}{A two-sided mgcv-style formula.}

\item{data}{Model data with one row per spatial observation.}

\item{basis}{A prepared \code{\link[=spde_basis]{spde_basis()}} object.}

\item{family}{An mgcv family object. The default is \code{mgcv::nb()}.}

\item{setting}{The model uses one \code{"global"} spatial score process.
\code{setting = "global_local"} is not supported.}

\item{coordinates}{Character vector naming the two coordinate columns.}

\item{precision_scale}{Scale on which the spatial log-precision prior is
defined. \code{"raw"} retains the original FEM precision parameterization.
\code{"observation"} normalizes each constrained spatial field to unit mean
marginal variance at the observed locations when its internal precision
equals one. A user-supplied proper prior then applies to this standardized
precision and generally changes the prior on the original FEM multiplier.
The default flat log prior is invariant to this constant log-scale shift.}

\item{G}{Optional frozen \code{gam.prefit} design, supplied instead of formula,
data and basis. Its full SPDE terms must satisfy the observation constraint.}

\item{control}{Named engine controls stored in the model and inherited by
\code{\link[=inlaST.estimate]{inlaST.estimate()}}. See that function for priors and numerical controls.}

\item{...}{Additional arguments passed to \code{mgcv::gam(..., fit = FALSE)}.}

\item{mesh}{Optional mesh selecting the native sparse setup: an
\code{\link[=spde_mesh]{spde_mesh()}}, an \code{fm_mesh_2d}, or an \code{fm_mesh_3d}. When supplied, the
single spatial term is built directly from \code{mesh}, \code{kappa} and
\code{coordinates}; \code{formula} then contains only the response, an optional
\code{offset()}, parametric terms, and admissible nuisance terms. The spatial
projector remains sparse; the bounded nuisance design is dense. See the
nuisance-smooth section. Mesh dimension (2 or 3) is detected automatically.}

\item{kappa}{Fixed positive spatial scale required by \code{mesh}. With
\code{alpha = 2} the Matern smoothness is \code{nu = 1} in 2D and \code{nu = 1/2} in 3D,
and the practical range is \code{sqrt(8 * nu) / kappa}.}
}
\value{
An \code{inlaST_model} for \code{\link[=inlaST.estimate]{inlaST.estimate()}}.
}
\description{
Constructs the same fixed-kappa SPDE score geometry as \code{\link[=model.set]{model.set()}} while
retaining raw sparse mesh precision matrices for INLA. Each spatial field is
always constrained to have zero mean at the observed locations.
}
\details{
Complete formulas and frozen \code{G} designs use the shared design
preparation of \code{\link[=mgcvST.set]{mgcvST.set()}}. Alternatively, supply SPDE component(s)
separately through \code{basis}, as in \code{\link[=model.set]{model.set()}}. Formula/basis setup enforces
the observation mean-zero constraint. A supplied \code{G} must already have a
compatible constrained SPDE design; otherwise rebuild it from formula/data.
Supported families are Gaussian with identity link, Poisson
with log link, and negative binomial with log link. Exactly one full
fixed-kappa SPDE basis is the tested spatial target, and it is the only
smooth in a frozen design. Native mesh setup also supports any number of
admissible iid nuisance blocks. Spatial mean-zero constraints cannot be
disabled. Observation weights and additional cross-penalties are not
supported.
}
\section{Nuisance smooths}{

The native INLA setup supports one spatial target and any number of
categorical \code{s(group, bs = "re")} terms or \code{s(..., bs = "gp")} terms whose
penalty is full rank after the observation intercept is projected out.
Each GP design and penalty are transformed together and whitened to an iid
block. Every iid block retains its estimated precision penalty in the small
nuisance \code{Vp} block alongside the unpenalized fixed effects. Numeric
random-effect groups, \verb{by=} nuisance terms, rank-deficient GP penalties and
other smooth bases are outside the native INLA model.

Parametric covariates are supported: numeric columns, factors and
their interactions all enter the nuisance design as fixed effects. A second
spatial (\code{bs = "spde"}) term is rejected, and the total nuisance design is
capped at 1000 columns.
}
