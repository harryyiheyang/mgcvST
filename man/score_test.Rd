% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mgcvst-api.R
\name{score_test}
\alias{score_test}
\title{Direct covariance score test for two SPDE mgcv fits}
\usage{
score_test(fit1, fit2, method = c("liu", "davies"), precision_tol = 1e-09)
}
\arguments{
\item{fit1, fit2}{Converged \code{mgcv::gam} fits with exactly one smooth, which
must inherit from \code{spde.smooth} or \code{spdePC.smooth}.}

\item{method}{Calibration method passed to
\code{\link[=rkhs_covariance_score_irls]{rkhs_covariance_score_irls()}}.}

\item{precision_tol}{Positive relative tolerance for basis and unscaled-Q
comparison. The default is \code{1e-9}.}
}
\value{
An \code{rkhs_covariance_score} object with conditional IRLS metadata.
}
\description{
Finds the sole SPDE smooth in each ordinary \code{mgcv::gam} fit, validates exact
row identifiers and relative agreement of the training basis and unscaled
precision matrices, and delegates to \code{\link[=rkhs_covariance_score_irls]{rkhs_covariance_score_irls()}}. No
score mathematics is reimplemented here.
}
\details{
The maximum absolute difference in \code{B} or unscaled \code{Q} must not exceed
\code{precision_tol * max(1, max(abs(M1)), max(abs(M2)))}. Thus changed
dimensions, coefficient order, row order, or fixed-kappa precision stop
with an error rather than being coerced.
}
