% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wgcna.R
\name{mgcvST.wgcna}
\alias{mgcvST.wgcna}
\alias{print.mgcvST_wgcna}
\title{Identify co-expression modules within explicitly selected gene blocks}
\usage{
mgcvST.wgcna(
  fitmgcvST,
  indices,
  wgcna.para = NULL,
  verbose = FALSE,
  threads = 1L
)

\method{print}{mgcvST_wgcna}(x, ...)
}
\arguments{
\item{fitmgcvST}{A compact fit returned by \code{\link[=mgcvST.estimate]{mgcvST.estimate()}} or
\code{\link[=inlaST.estimate]{inlaST.estimate()}}.}

\item{indices}{Required gene IDs, integer positions in \code{fitmgcvST$feature_id},
or a named list of such vectors. Each block must contain at least two distinct
genes. A vector defines the block named \code{selected}. Gene order is preserved.}

\item{wgcna.para}{\code{NULL}, an empty list, or named partial overrides of:
\code{networkType = "signed"}, \code{power = 6}, \code{TOMType = "signed"},
\code{hclustMethod = "average"}, \code{minClusterSize = 20L}, \code{deepSplit = 1L}.
Unspecified settings keep these defaults; unknown names are errors.
Blocks smaller than \code{minClusterSize} retain their matrices and receive
grey (zero) labels, without changing the requested module size.}

\item{verbose}{Whether to display compact progress messages.}

\item{threads}{Positive OpenMP thread count used by sparse INLA score
construction. It has no effect on mgcv score construction.}

\item{x}{An \code{mgcvST_wgcna} result.}

\item{...}{Unused.}
}
\value{
An \code{mgcvST_wgcna} object with \code{modules} (component, feature ID, integer
module, color), named \code{networks} (feature IDs, covariance, correlation,
adjacency, TOM, tree, labels, modules, coordinate count \code{q}, and status),
\code{score} (aligned \code{A}, group names and widths), \code{settings}, and \code{timing}.
A zero module label means unassigned (grey). Module labels are local to
each input block. No biological annotations are inferred automatically.
}
\description{
Constructs the original score covariance \code{crossprod(A) / nrow(A)} from an
\code{\link[=mgcvST.estimate]{mgcvST.estimate()}} or \code{\link[=inlaST.estimate]{inlaST.estimate()}} fit, converts it to correlation,
and performs WGCNA. \code{indices} selects genes, for example the genes in a
connected component
identified after \code{\link[=mgcvST.test]{mgcvST.test()}}. A named list analyzes several gene blocks
separately. It never pools blocks, selects significant pairs, or reruns
estimation or testing. All pairs within each selected block enter its COV.
}
\details{
Shared SPDE factors are constructed once. All backends compute only the score
vectors needed here; pair-test calibration matrices are unnecessary. The
result retains these vectors and the matrices needed for downstream analysis
without modifying the supplied fit. No centering, ridge, or covariance
projection is applied.

The score coordinates of the single spatial component are divided by their
coordinate count, as in the score covariance definition.

An \code{\link[=inlaST.estimate]{inlaST.estimate()}} fit is accepted here and \strong{dispatched to the sparse
INLA score kernel}, which is what \code{\link[=inlaST.wgcna]{inlaST.wgcna()}} calls directly. The two
entry points therefore return the same result for the same INLA fit;
\code{mgcvST.wgcna()} stays accepting so existing INLA code keeps working, and
\code{\link[=inlaST.wgcna]{inlaST.wgcna()}} exists so INLA callers can name the sparse path explicitly
and get INLA-specific argument checking.
}
\examples{
\dontrun{
W <- mgcvST.wgcna(fit, indices = genes)
W <- mgcvST.wgcna(fit, indices = list(block1 = genes1, block2 = genes2),
                  wgcna.para = list(power = 6))
W$modules
W$networks$block1$covariance
}
}
\seealso{
\code{\link[=inlaST.wgcna]{inlaST.wgcna()}} for the explicit sparse INLA entry point.
}
