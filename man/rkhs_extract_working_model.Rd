% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score-irls.R
\name{rkhs_extract_working_model}
\alias{rkhs_extract_working_model}
\title{Extract the final IRLS working model from an mgcv fit}
\usage{
rkhs_extract_working_model(fit)
}
\arguments{
\item{fit}{A converged \code{mgcv::gam} fit.}
}
\value{
A list with working response, working variance, offset, family
metadata, and convergence information.
}
\description{
Uses the generic final-PIRLS identities
\deqn{z=\eta+(y-\mu)g'(\mu),\qquad
W_i=w_i^{prior}/\{V(\mu_i)g'(\mu_i)^2\},}
and returns the diagonal working variance \code{phi / W}. For binomial matrix
responses, \code{mgcv} stores proportions in \code{fit$y} and trials in the prior
weights, so the same expression applies.
}
