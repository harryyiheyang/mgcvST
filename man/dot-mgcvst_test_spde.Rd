% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mgcvst-api.R
\name{.mgcvst_test_spde}
\alias{.mgcvst_test_spde}
\title{Covariance score tests for an explicit pair universe}
\usage{
.mgcvst_test_spde(
  fitmgcvST,
  q.value = 0.05,
  FDR = TRUE,
  method = "BH",
  BPPARAM = BiocParallel::SerialParam(),
  ...,
  pairs = NULL,
  highlight = NULL,
  calibration = c("liu", "davies"),
  chunk_size = NULL,
  threads = NULL,
  verbose = FALSE
)
}
\arguments{
\item{fitmgcvST}{A compact object returned by \code{\link[=mgcvST.estimate]{mgcvST.estimate()}}.}

\item{q.value}{Pair-covariance discovery threshold in \verb{(0, 1]}.}

\item{FDR}{Logical; if \code{TRUE}, adjust pair p-values before applying
\code{q.value}.}

\item{method}{Method passed to \code{stats::p.adjust()} when \code{FDR = TRUE}.}

\item{BPPARAM}{A \code{BiocParallelParam}; defaults to \code{SerialParam()}.}

\item{...}{Reserved for future score options; currently unused arguments
are rejected.}

\item{pairs}{Required two-column tested-pair universe using feature IDs or
one-based indices.}

\item{highlight}{Optional two-column pairs that must be tested and retained.
They may overlap \code{pairs}; non-overlapping highlights are appended.}

\item{calibration}{Score calibration passed to \code{\link[=rkhs_score_calibrate]{rkhs_score_calibrate()}}.}

\item{chunk_size}{Positive number of tested pairs per task or C++ block.
Liu calibration defaults to 10,000 pairs per interruptible block.}

\item{threads}{Positive number of OpenMP threads for the Liu C++ kernel.
\code{NULL} uses \code{bpworkers(BPPARAM)} without launching Snow workers.}

\item{verbose}{Logical; report Liu summary and block progress.}
}
\value{
A compact \code{mgcvST_test} object containing pair results, adjusted
p-values, discovery/highlight/retention flags, threshold metadata, and
timing metadata.
}
\description{
Tests every pair in \code{pairs}, plus any additional \code{highlight} pairs, using
the fixed compact summaries in \code{fitmgcvST}. The primary statistic is the
squared signed score \code{U^2}. The default Liu calibration matches the first
four moments of \code{U^2} directly from trace powers of \verb{H1 H2}, without a
per-pair spectral decomposition. Davies remains available when explicitly
requested for a smaller pair universe. No GAM is refitted.
}
\details{
\code{q.value}, \code{FDR}, and \code{method} apply to the resulting pair-covariance
p-values. Automatic discoveries are the pairs passing that threshold.
Highlighted pairs are always retained in the returned set regardless of
significance. When one feature or pair cannot be evaluated, its numerical
fields remain \code{NA} and its concrete error message is retained without
interrupting the other pairs. The
function does not generate all pairwise combinations: callers must define
the tested universe, which prevents accidental allocation of
\code{choose(10000, 2)} rows.

Under Liu calibration, each feature summary is constructed once. Dense pair
universes use \code{CppMatrix::matrixMultiply(..., transA = TRUE)} for all signed
scores, followed by bounded, multithreaded internal C++ blocks for the Liu trace
moments. This avoids copying the full collection of \code{H} matrices to Snow
workers. Other calibrations retain BiocParallel pair chunks.
}
\keyword{internal}
